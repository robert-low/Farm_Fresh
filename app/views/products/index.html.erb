<h1>All products</h1>

<% @products.each do |product| %>
    <%= link_to product_path(product) do %>
        <div class="card">
            <%= product.name %>
            <p>Description: <%= product.overview %></p>
            <p>£<%= product.price %></p>
        </div>
    <% end %>
<% end %>
