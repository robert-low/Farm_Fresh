<div class = "index-head text-center">
    <h1>Welcome to FarmFresh</h1>
    <h3>Rent the freshest fake fruit from  your friendliest farm!<h3>
  </div>
   <% if params[:query].blank? %>
      <h3><%= pluralize @products.count, 'product' %> available</h1>
    <% else %>
      <h3><%= pluralize @products.count, 'result' %> for <%= params[:query] %></h1>
    <% end %>
  <div class="main_container">
  <div class="content">
    <div data-refresh-list-target="list" class = "cards">
      <%= render 'list', products: @products %>
    </div>
  </div>
  <div id="map">
      <div style="width: 100%; height: 100%;"
        data-controller="map"
        data-map-markers-value="<%= @markers.to_json %>"
        data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
    </div>
</div>
